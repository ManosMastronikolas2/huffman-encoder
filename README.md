# huffman-encoder
A simple Huffman encoder for text files ( is gonna be expanded for other binary files ) written in C.

After making file, go to generated bin directory and run:

./huffman <in_file> <out_file>
